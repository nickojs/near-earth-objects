import styled from 'styled-components';
import { Container as C } from '../../generalStyles';

export const Container = styled(C)`
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;

  width: 50%; padding: 12px; margin: 12px auto;
`;
